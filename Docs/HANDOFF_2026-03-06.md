# Handoff - 2026-03-06

## Scope Completed

VFX Graph reliability and coverage pass completed across Unity plugin and server test surfaces.

Follow-up on the same date also completed the Unity-side V2/V3 live bridge pass: previously Python-visible but Unity-unsupported commands are now compiling, registering, and executing against the active `BattleStreet` editor.

## Patched Areas

### Unity plugin

- `MCPForUnity/Editor/MCPForUnity.Editor.asmdef`
  - Added `Unity.VisualEffectGraph.Runtime` / `Unity.VisualEffectGraph.Editor` references.
  - Added version define: `com.unity.visualeffectgraph -> UNITY_VFX_GRAPH`.
- `MCPForUnity/Editor/Tools/Vfx/VfxGraphAssets.cs`
  - Replaced fixed `12.1.x` gate with minimum-version support logic.
- `MCPForUnity/Editor/Tools/Vfx/VfxGraphWrite.cs`
  - Added `Has*` checks for scalar/vector/gradient/texture/mesh/curve writes.
  - Missing parameter names now return clean MCP error responses.
- `MCPForUnity/Editor/Tools/Vfx/VfxGraphRead.cs`
  - Added best-effort exposed parameter introspection in `vfx_get_info`.
- `MCPForUnity/Editor/Tools/LiveV2V3Tools.cs`
  - Fixed compile blockers that prevented reflection discovery from ever registering the new V2/V3 handlers.
  - Replaced invalid `PlayerSettings.GetDefaultRenderingPath` usage with render-pipeline-safe reporting.
  - Switched playbook/benchmark internal dispatch to `CommandRegistry.InvokeCommandAsync(...)`.
  - Normalized snake_case and camelCase payload names for `run_playbook` and `run_benchmark`.
  - Optimized `search_assets_advanced` to skip importer/dependency/label work unless required.
  - Strengthened `ResolveByString(...)` to fall back to `GameObjectLookup.SearchGameObjects(..., includeInactive: true)` so `focus_hierarchy` resolves the same smoke targets that `find_gameobjects` can find.
- `MCPForUnity/Editor/Tools/Visual/ManageScreenshot.cs`
  - Fixed nullable `Vector3?` camera parse results being passed into `CaptureSceneViewFromAngle(...)` by dereferencing only after the existing `HasValue` guard.

### Server/tests

- `Server/tests/test_manage_vfx_actions.py`
  - Added explicit coverage for:
    - `vfx_set_float`
    - `vfx_set_int`
    - `vfx_set_vector4`
    - `vfx_set_gradient`
    - `vfx_set_mesh`
  - Added action-case normalization coverage.
- `Server/tests/integration/test_manage_vfx_live_setter_matrix.py`
  - Added schema-aware setter matrix helper with deterministic mocked pass/skip behavior.
  - Added opt-in live test gate with `UNITY_MCP_RUN_LIVE_VFX_MATRIX=1`.

### Scripts/docs

- Added `Scripts/Create-MageFireballVfx.ps1` for reproducible live VFX smoke setup.
- Added `Server/tests/integration/test_live_unity_smoke_runner.py` for opt-in reusable live Unity HTTP smoke validation.
- Added `Scripts/Run-LiveUnitySmoke.ps1` as a one-command wrapper for the live smoke matrix.
- Added `Scripts/Run-ValidationSuite.ps1` to run targeted server-side validation plus live Unity smoke in one flow.
- Added `Docs/MAGE_FIREBALL_VFX.md` for usage and troubleshooting.
- Updated `Docs/LIVE_TEST_MATRIX.md` with matrix helper and VFX operational notes.

## Validation Performed

- `pytest tests/test_manage_vfx_actions.py -q` -> pass
- `pytest tests/test_manage_vfx_actions.py tests/test_cli.py -k "vfx" -q` -> pass
- `pytest tests/integration/test_manage_vfx_live_setter_matrix.py tests/test_manage_vfx_actions.py tests/test_cli.py -k "vfx" -q` -> pass (live test path skipped unless explicitly enabled)
- Live CLI commands verified against connected `BattleStreet` instance for fireball setup and retuning.
- Live CLI verification against `BattleStreet (Unity 6000.3.8f1) [63b48c2d]` now also confirms the V2/V3 bridge surface executes successfully:
  - `manage_project_settings`
  - `manage_editor_settings`
  - `manage_player_settings`
  - `manage_build_settings`
  - `manage_define_symbols`
  - `manage_asset_import_settings`
  - `manage_import_pipeline`
  - `manage_registry_config`
  - `navigate_editor`
  - `reveal_asset`
  - `focus_hierarchy`
  - `frame_scene_target`
  - `open_inspector_target`
  - `search_assets_advanced`
  - `wait_for_editor_condition`
  - `start_trace`
  - `subscribe_editor_events`
  - `manage_transactions`
  - `run_playbook` (dry-run)
  - `run_benchmark`
- Reusable validation entry points now verified:
  - `python -m pytest tests/integration/test_live_unity_smoke_runner.py -q`
  - `Scripts/Run-LiveUnitySmoke.ps1 -UnityInstance BattleStreet@63b48c2db2e73e3e`
- Final rerun after the hierarchy resolver fix:
  - targeted server validation: `213 passed in 33.78s`
  - live Unity smoke: `1 passed, 1 deselected in 0.59s`

## Root Cause Summary

- The original `Unknown or unsupported command type` failures were misleading: the Python catalog was fine, but Unity never registered the bridge handlers because `LiveV2V3Tools.cs` was failing to compile.
- The decisive diagnostic signal came from the live Unity console after refresh, not from static editor diagnostics.
- After compile recovery, the remaining failures were contract-shape and performance issues rather than missing registration.
- The last live blocker was narrower: `focus_hierarchy` still failed after the bridge recovery because its string-based Unity resolver path was weaker than `find_gameobjects`, so inactive/path-based scene objects could be discovered by smoke setup but not focused in the Hierarchy.

## Known Operational Gotchas

- Duplicate GameObject names can route VFX actions to the wrong object when using `by_name`.
- For negative instance IDs, use `--` before the target ID in CLI calls.
- Square-card particle visuals are typically material/render-mode related; use textured alpha falloff and non-billboard settings where possible.
- Unity domain reloads during plugin recompilation can temporarily drop the editor instance from the local MCP server. Retry after reconnect before treating this as a persistent transport failure.
- For the bridged V2/V3 tool surface, prefer keeping Python/server payload naming aligned with Unity aliases. Snake_case compatibility is now handled for the playbook and benchmark entry points.

## Recommended Next Steps

1. Add a dedicated non-square particle material preset under source-controlled assets for stable visuals across scenes.
2. Extend the live matrix helper or CLI probe harness to emit a markdown artifact for the newly live V2/V3 bridge surface.
3. Clean up the remaining obsolete Unity API warnings in `LiveV2V3Tools.cs` now that functional coverage is restored.
4. Keep `Docs/LIVE_TEST_MATRIX.md` updated whenever new live V2/V3 action coverage is added.
